// Precisa de 3 parametros
// for(variavel de condição;expressão logica; controle){verdade}falso sai do loop

for (let i = 0; i <= 10; i++) {
    console.log(`jhonn : ${i}`)
    if (i % 2 == 0) {
        console.log(`${i} é Par`)
    } else {
        console.log(`${i} é Impar`)
    }
}